name: validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  schemas:
    name: JSON Schemas
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Install ajv-cli
        run: npm i -g ajv-cli

      - name: Validate fixtures vs schemas
        run: bash tests/validate-schemas.sh

      - name: Test load-config.sh
        run: bash tests/test-load-config.sh

      - name: Test setup-product-dir.sh
        run: bash tests/test-setup-product-dir.sh

      - name: Test update-index.sh
        run: bash tests/test-update-index.sh

      - name: Test update-progress.sh
        run: bash tests/test-update-progress.sh

      - name: Test telemetry.sh
        run: bash tests/test-telemetry.sh

      - name: Test ask-or-default.sh
        run: bash tests/test-ask-or-default.sh

      - name: Test apply-naming.sh
        run: bash tests/test-apply-naming.sh

      - name: Test detect-test-commands.sh
        run: bash tests/test-detect-test-commands.sh

      - name: Test check-mcp-required.sh
        run: bash tests/test-check-mcp-required.sh

      - name: Test detect-platforms.sh
        run: bash tests/test-detect-platforms.sh

      - name: Test setup-config.sh
        run: bash tests/test-setup-config.sh

      - name: Test run-lifecycle-script.sh
        run: bash tests/test-run-lifecycle-script.sh

      - name: Test tickets-adapter.sh
        run: bash tests/test-tickets-adapter.sh

      - name: Test docs-adapter.sh
        run: bash tests/test-docs-adapter.sh

      - name: Test frame0-helper.sh
        run: bash tests/test-frame0-helper.sh

      - name: Validate agents frontmatter
        run: bash tests/test-agents.sh

      - name: Test parse-agent-output.sh
        run: bash tests/test-parse-agent-output.sh

  shellcheck:
    name: Shell scripts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Run shellcheck
        run: |
          # Shell files only (skip .tpl templates which use {{vars}} placeholders).
          # --severity=warning skips info/style (intentional [ ] && ok || ko in tests).
          shopt -s globstar nullglob
          files=(skills/_shared/**/*.sh tests/**/*.sh)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No shell scripts to check yet."
            exit 0
          fi
          shellcheck --severity=warning "${files[@]}"

  json-syntax:
    name: JSON syntax
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Validate JSON files parse
        run: |
          set -e
          fail=0
          while IFS= read -r f; do
            if ! jq empty "$f" >/dev/null 2>&1; then
              echo "INVALID JSON: $f"
              fail=1
            fi
          done < <(find . -name '*.json' -not -path './node_modules/*' -not -path './.git/*')
          exit $fail
