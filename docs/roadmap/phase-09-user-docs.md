# Phase 9 — Documentation utilisateur

**Objectif:** docs install + usage public.

- [x] README.md repo (quickstart 5 min) — v1.0.0 status + lien install/getting-started
- [x] `docs/install.md` (marketplace `bryanberger` + clone manuel + projet-scoped)
- [x] `docs/getting-started.md` (premier `/snap:init` → `/snap:define` walkthrough)
- [x] `docs/troubleshooting.md` (auth MCP, secrets, resume, sync, version mismatch)
- [x] `docs/configuration.md` (référence `snapship.config.json` complète — `git mv` depuis `config.md`)
- [ ] Screencast court (asciinema) flux `/define` → `/develop` → `/qa` — _hors-scope session texte, à enregistrer manuellement_
- [x] Migration guide v0.6 → v1.0 (`docs/migration-v1.md`)

**Sortie:** user autonome install + premier run sans support.
