# Phase 8 — Dogfooding

**Objectif:** valider sur projet réel avant publication marketplace. **Prérequis : Phase 7.5 livrée.**

- [ ] Choisir projet pilote (green-field idéal: app side-project user)
- [ ] Run cycle complet `/define` → `/ticket` → `/wireframe` → `/design` → `/develop` → `/qa` sur 1-2 features réelles
- [ ] Capture friction points (steps lents, prompts ambigus, échecs MCP)
- [ ] Itérer fixes critiques (P0)
- [ ] Telemetry analyse: identifier cycles fréquents, retries, durées

**Sortie:** v0.5.0-rc validée prod use, backlog P1+ ouvert.
